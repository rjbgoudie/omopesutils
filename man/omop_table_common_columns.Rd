% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omop-metadata.R
\name{omop_table_common_columns}
\alias{omop_table_common_columns}
\title{Columns to join an OMOP table to a related table on}
\usage{
omop_table_common_columns(table, fk_table)
}
\arguments{
\item{table}{OMOP table name}

\item{fk_table}{OMOP table name of the referenced table, in lower case}
}
\value{
A character vector of column names.
}
\description{
The columns of \code{table} that reference \code{fk_table}. When \code{table} and
\code{fk_table} are the same, the primary key of \code{table} is returned instead,
since a table is joined to itself on its own key.
}
\details{
For example, \code{omop_table_common_columns("visit_occurrence", "person")} is
\code{"person_id"}, whereas
\code{omop_table_common_columns("visit_occurrence", "visit_occurrence")} is
\code{"visit_occurrence_id"}.

The result is de-duplicated, so a repeated reference yields each column
once. In the v5.4 specification no table references a \emph{different} table
through more than one column, so in practice a single column name is
returned. Note that the returned columns are those of \code{table}; they are
assumed to be named identically in \code{fk_table} (or, in the case of
\code{\link[=omop_es_tbl_with_links]{omop_es_tbl_with_links()}}, in the corresponding OMOP-ES \verb{_links} table).
}
\seealso{
Other OMOP CDM metadata:
\code{\link[=omop_all_tables]{omop_all_tables()}},
\code{\link[=omop_cdm_tables]{omop_cdm_tables()}},
\code{\link[=omop_metadata_field_level]{omop_metadata_field_level()}},
\code{\link[=omop_metadata_table_level]{omop_metadata_table_level()}},
\code{\link[=omop_source_tables_for_foreign_key_columns]{omop_source_tables_for_foreign_key_columns()}},
\code{\link[=omop_table_all_key_columns]{omop_table_all_key_columns()}},
\code{\link[=omop_table_columns]{omop_table_columns()}},
\code{\link[=omop_table_primary_key]{omop_table_primary_key()}},
\code{\link[=omop_vocab_tables]{omop_vocab_tables()}}
}
\concept{OMOP CDM metadata}
\keyword{internal}
