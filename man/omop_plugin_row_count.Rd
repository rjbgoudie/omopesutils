% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omop-count.R
\name{omop_plugin_row_count}
\alias{omop_plugin_row_count}
\title{Row counts of every OMOP table in a schema, by OMOP-ES plugin}
\usage{
omop_plugin_row_count(db, schema_public = "dbo", schema_private = "priv")
}
\arguments{
\item{db}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object, as returned by \code{\link[DBI:dbConnect]{DBI::dbConnect()}}.}

\item{schema_public}{Name of the schema holding the public OMOP tables}

\item{schema_private}{Name of the schema holding the private OMOP-ES
\verb{_links} tables}
}
\value{
A tibble with columns \code{table}, \code{plugin} and \code{row_count}, with one
row per table and plugin.
}
\description{
As \code{\link[=omop_tables_row_count]{omop_tables_row_count()}}, but attributing each row to the OMOP-ES
plugin that produced it. This makes it possible to see which mapper is
responsible for the rows in a table.
}
\details{
The plugin is read from the \code{links__plugin_provenance} column that
\code{\link[=omop_es_tbl_with_links]{omop_es_tbl_with_links()}} joins on from the table's own \verb{_links} table.
Where a table has no such column --- because it has no \verb{_links} table, or
none that can be joined --- all of its rows are attributed to a plugin
named \code{"default"}, so that every table appears in the result.

As with \code{\link[=omop_tables_row_count]{omop_tables_row_count()}}, only tables that are part of the OMOP
CDM and present in the database are counted.
}
\seealso{
Other row counts:
\code{\link[=omop_diff_plugins_row_count]{omop_diff_plugins_row_count()}},
\code{\link[=omop_diff_tables_row_count]{omop_diff_tables_row_count()}},
\code{\link[=omop_tables_row_count]{omop_tables_row_count()}}
}
\concept{row counts}
\keyword{internal}
