% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diff-cache.R
\name{git_resolve_run_sha}
\alias{git_resolve_run_sha}
\title{Resolve the commit that a branch will be run at}
\usage{
git_resolve_run_sha(branch, omop_es_path)
}
\arguments{
\item{branch}{A git branch, tag or SHA}

\item{omop_es_path}{Path to OMOP-ES directory}
}
\value{
A length-one character vector containing a full 40-character SHA.
}
\description{
Works out which commit \code{\link[=omop_es_run_git_sha]{omop_es_run_git_sha()}} will end up running for
\code{branch}, without checking anything out.
}
\details{
\code{\link[=omop_es_run_git_sha]{omop_es_run_git_sha()}} checks \code{branch} out and fast-forwards it to its
upstream before running, so the commit that gets run is the \emph{upstream's}
commit, not the one the local branch currently points at. This predicts
that outcome:
\itemize{
\item if \code{branch} has an upstream, and that upstream is a descendant of the
local branch, the upstream's commit is returned --- that is what
fast-forwarding produces
\item otherwise the commit \code{branch} resolves to is returned, which covers a
branch with no upstream, a tag, a raw SHA, and the case where the
upstream has diverged and \code{\link[=omop_es_run_git_sha]{omop_es_run_git_sha()}} will refuse to merge
}

Fetch first if the prediction needs to account for commits that are on the
remote but not yet in the local repository.

The prediction is used only to look for a cache entry. What gets \emph{stored}
is always keyed by the commit that was actually checked out, read back from
\code{HEAD} after the run, so a wrong prediction costs a needless pipeline run
rather than a mislabelled extract.
}
\seealso{
Other cached pipeline runs:
\code{\link[=diff_cache_entry]{diff_cache_entry()}},
\code{\link[=diff_cache_read]{diff_cache_read()}},
\code{\link[=diff_cache_write]{diff_cache_write()}},
\code{\link[=diff_extract_subdir]{diff_extract_subdir()}},
\code{\link[=omop_es_run_cached]{omop_es_run_cached()}}
}
\concept{cached pipeline runs}
\keyword{internal}
