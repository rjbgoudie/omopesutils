% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omop-cross-tabs.R
\name{omop_concept_column_summaries}
\alias{omop_concept_column_summaries}
\title{Summarise how each concept column has been populated}
\usage{
omop_concept_column_summaries(cross_tabulations)
}
\arguments{
\item{cross_tabulations}{A collected cross-tabulation, as returned by
\code{\link[=omop_cross_tabulation]{omop_cross_tabulation()}}.}
}
\value{
A tibble with one row per \code{table} and \code{column}, and columns
\code{vocabularies} (a list of vocabulary ids), \code{vocabularies_text} (those
vocabularies as HTML links with row counts), \code{n_row}, \code{n_zero_or_na} and
\code{n_distinct_concepts}.
}
\description{
Reduces a cross-tabulation to one row per column, counting the rows, the
distinct concepts, and the rows that are unmapped, and listing the
vocabularies the concepts came from. This is what the column-level table of
the summary report is built from.
}
\details{
Each column stem in the cross-tabulation describes two real OMOP columns,
so this summarises both: once for \verb{<stem>_concept_id} and once for
\verb{<stem>_source_concept_id}, using the corresponding concept and vocabulary
columns, and stacks the two.

A row counts as unmapped when its concept id is \code{0} --- the OMOP convention
for "no matching concept" --- or \code{NA}. Comparing \code{n_zero_or_na} with
\code{n_row} therefore tells you whether a column is entirely unmapped, which is
how \code{\link[=omop_es_field_level_summary_html]{omop_es_field_level_summary_html()}} decides to mark a column "Not
implemented".

The vocabularies are ordered by how many rows they account for, so the
dominant vocabulary appears first, and each is rendered as an Athena link
with its row count by \code{\link[=athena_vocab_link]{athena_vocab_link()}}.
}
\seealso{
Other concept cross-tabulation:
\code{\link[=omop_concept_summary]{omop_concept_summary()}},
\code{\link[=omop_concept_summary_all]{omop_concept_summary_all()}},
\code{\link[=omop_cross_tabulation]{omop_cross_tabulation()}},
\code{\link[=omop_table_cross_tabulation]{omop_table_cross_tabulation()}}
}
\concept{concept cross-tabulation}
\keyword{internal}
