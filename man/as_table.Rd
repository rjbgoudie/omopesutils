% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-dbplyr.R
\name{as_table}
\alias{as_table}
\title{Materialise a lazy query as a named database table}
\usage{
as_table(x, table_name)
}
\arguments{
\item{x}{A lazy \code{tbl} with a database source}

\item{table_name}{Name to create the table under, a character string.
Interpolated into the SQL as-is, so it must be a safe identifier.}
}
\value{
A lazy \code{tbl} for the created table.
}
\description{
Renders a \pkg{dbplyr} lazy query to SQL and runs it as
\verb{CREATE OR REPLACE TABLE <table_name> AS ...}, returning a lazy \code{tbl} for
the newly created table.
}
\details{
This is used by \code{\link[=omop_es_diff_viewer]{omop_es_diff_viewer()}} to materialise each side of a
comparison before taking the set difference between them, which otherwise
exhausts duckdb's memory.

Unlike \code{\link[dplyr:compute]{dplyr::compute()}}, the table is created under a name chosen by the
caller, and any existing table of that name is replaced. The table is not
temporary and is not cleaned up, so callers should reuse a fixed name
rather than generating new ones.
}
\keyword{internal}
