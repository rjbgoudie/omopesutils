% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viewer.R
\name{omop_es_viewer}
\alias{omop_es_viewer}
\title{Browse a single OMOP-ES extract}
\usage{
omop_es_viewer(db, links_patient_id_column)
}
\arguments{
\item{db}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object with an OMOP-ES extract registered
in the \code{dbo} and \code{priv} schemas, as by
\code{\link[=duckdb_register_omop_es_output]{duckdb_register_omop_es_output()}}}

\item{links_patient_id_column}{Name of the patient identifier column in the
OMOP-ES \code{person} \verb{_links} table, without the \code{links__person__} prefix.
Used to label and populate the patient picker. Required.}
}
\value{
A shiny app object, as returned by \code{\link[shiny:shinyApp]{shiny::shinyApp()}}.
}
\description{
Launches a \pkg{shiny} application for browsing the tables of one OMOP-ES
extract, with the OMOP-ES \verb{_links} tables joined on. It is a convenient way
to look at what the pipeline actually produced without writing queries.
}
\details{
The table picker offers every table and view in the \code{dbo} schema (see
\code{\link[=dbListTablesAndViewsInSchema]{dbListTablesAndViewsInSchema()}}), and the selected table is read with
\code{\link[=omop_es_tbl_with_links]{omop_es_tbl_with_links()}}, so the OMOP-ES provenance and source-system
columns are available alongside the OMOP columns. The sidebar allows the
rows to be restricted to particular patients, the \verb{links__*} columns to be
hidden, and the page size to be chosen; it also reports how many rows match
before paging.

Paging is done in the database rather than in the browser: rows are ordered
by the table's primary key (see \code{\link[=omop_table_primary_key]{omop_table_primary_key()}}) with
\code{\link[dbplyr:window_order]{dbplyr::window_order()}} and then sliced by row number, so only one page is
ever collected. This is what makes it usable on tables too large to bring
into R.

The patient identifier column is passed by name rather than hard-coded, and
is looked up as \verb{links__person__<links_patient_id_column>} --- the name
\code{\link[=omop_es_tbl_with_links]{omop_es_tbl_with_links()}} gives it after joining the \code{person} \verb{_links}
table. This keeps identifiable source-system column names out of this open
source package.
}
\section{Known limitations}{


The Previous and Next buttons, and the unused \code{page_info} output, refer to
a \code{total_rows} object that is not defined in the function, so paging past
the first page raises an error. The schema is also hard-coded: the table
picker reads \code{dbo}, and \code{\link[=omop_es_tbl_with_links]{omop_es_tbl_with_links()}} is called with its
default \code{dbo}/\code{priv} schemas, so this cannot be pointed at an extract
registered under other schema names.
}

\examples{
\dontrun{
db <- DBI::dbConnect(duckdb::duckdb())
duckdb_register_omop_es_output(db, extract_path, omop_es_path)
omop_es_viewer(db, links_patient_id_column = "my_patient_id_column")
}
}
\seealso{
\code{\link[=omop_es_diff_viewer]{omop_es_diff_viewer()}} for comparing two extracts.

Other OMOP-ES extract viewers:
\code{\link[=omop_es_diff_viewer]{omop_es_diff_viewer()}},
\code{\link[=omop_es_diff_viewer_local]{omop_es_diff_viewer_local()}},
\code{\link[=omop_es_diff_viewer_local_git]{omop_es_diff_viewer_local_git()}}
}
\concept{OMOP-ES extract viewers}
