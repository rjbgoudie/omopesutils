% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omop-tbl.R
\name{dbListOmopTables}
\alias{dbListOmopTables}
\title{List the OMOP tables in a schema}
\usage{
dbListOmopTables(conn, schema = "dbo", exclude_vocab = TRUE)
}
\arguments{
\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object, as returned by \code{\link[DBI:dbConnect]{DBI::dbConnect()}}.}

\item{schema}{Name of the schema to list}

\item{exclude_vocab}{Whether to exclude the OMOP vocabulary tables (see
\code{\link[=omop_vocab_tables]{omop_vocab_tables()}}). These are usually excluded because they are large,
shared between extracts, and not of interest when inspecting or diffing
an extract.}
}
\value{
A character vector of table or view names.
}
\description{
Lists the tables and views present in \code{schema}, optionally excluding the
OMOP vocabulary tables. Views are included, which matters because
\code{\link[=duckdb_register_omop_es_output]{duckdb_register_omop_es_output()}} registers an extract as views rather
than as tables.
}
\details{
Note that this lists what is actually \emph{in} the schema, and so may include
tables that are not part of the OMOP CDM (for example the tables OMOP-ES
writes to its \code{custom} directory). It is not filtered against
\code{\link[=omop_all_tables]{omop_all_tables()}}.
}
\examples{
\dontrun{
db <- DBI::dbConnect(duckdb::duckdb())
duckdb_register_omop_es_output(db, extract_path, omop_es_path)
dbListOmopTables(db, "dbo")
}
}
\seealso{
Other database schema helpers:
\code{\link[=dbCreateSchema]{dbCreateSchema()}},
\code{\link[=dbListTablesAndViewsInSchema]{dbListTablesAndViewsInSchema()}},
\code{\link[=dbListTablesInSchema]{dbListTablesInSchema()}}
}
\concept{database schema helpers}
