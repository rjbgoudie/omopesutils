% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omop-cross-tabs.R
\name{omop_cross_tabulation}
\alias{omop_cross_tabulation}
\title{Cross-tabulate the concept columns of every OMOP table}
\usage{
omop_cross_tabulation(db)
}
\arguments{
\item{db}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object with an OMOP-ES extract
registered, as by \code{\link[=duckdb_register_omop_es_output]{duckdb_register_omop_es_output()}}.}
}
\value{
A lazy \code{tbl} with the columns described in
\code{\link[=omop_table_cross_tabulation]{omop_table_cross_tabulation()}}.
}
\description{
Runs \code{\link[=omop_table_cross_tabulation]{omop_table_cross_tabulation()}} over every OMOP table in the public
schema and stacks the results, giving one table that describes how every
concept column in the extract has been populated. This is the input the
extract summary report is built from.
}
\details{
\code{visit_occurrence_ext_fce} is excluded, because its concept tables are
already joined on.

The result is a lazy \code{tbl}: it is a \verb{UNION ALL} of one query per table, and
is potentially expensive, so collect it once and reuse it rather than
recomputing it per section of a report.
}
\examples{
\dontrun{
db <- DBI::dbConnect(duckdb::duckdb())
duckdb_register_omop_es_output(db, extract_path, omop_es_path)
cross_tabulations <- dplyr::collect(omop_cross_tabulation(db))
}
}
\seealso{
\code{\link[=omop_es_extract_summary_report]{omop_es_extract_summary_report()}}, which renders this.

Other concept cross-tabulation:
\code{\link[=omop_concept_column_summaries]{omop_concept_column_summaries()}},
\code{\link[=omop_concept_summary]{omop_concept_summary()}},
\code{\link[=omop_concept_summary_all]{omop_concept_summary_all()}},
\code{\link[=omop_table_cross_tabulation]{omop_table_cross_tabulation()}}
}
\concept{concept cross-tabulation}
