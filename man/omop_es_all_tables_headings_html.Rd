% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-summary.R
\name{omop_es_all_tables_headings_html}
\alias{omop_es_all_tables_headings_html}
\title{Build the per-table headings of the extract summary report}
\usage{
omop_es_all_tables_headings_html(cross_tabulations, plugin_metadata)
}
\arguments{
\item{cross_tabulations}{A collected cross-tabulation, as returned by
\code{\link[=omop_cross_tabulation]{omop_cross_tabulation()}}.}

\item{plugin_metadata}{Plugin metadata in table-major form, i.e. the result
of \code{\link[=omop_es_plugins_extract_metadata]{omop_es_plugins_extract_metadata()}} passed through
\code{\link[purrr:list_transpose]{purrr::list_transpose()}}, so that it is named by OMOP table.}
}
\value{
A list of \code{\link[htmltools:tagList]{htmltools::tagList()}}s, named by OMOP table.
}
\description{
One heading block per table covered by the report, each a horizontal rule
and a top-level heading, so that the report reads as a sequence of
per-table sections.
}
\details{
Like the other \verb{omop_es_*_html()} builders, this returns a list named by
OMOP table rather than a single blob of HTML. The report template collects
the builders' lists and transposes them, which interleaves the sections so
that each table's heading, documentation, column summary, tabulations and
provenance appear together. Adding a section to the report therefore means
adding another builder that is named by table in the same way.

A builder may also contribute nothing: the template drops the sections that
\code{include_private = FALSE} excludes before transposing, so a builder that is
not called simply leaves its section out of every table.

The tables covered are those given by
\code{\link[=omop_es_extract_summary_all_tables]{omop_es_extract_summary_all_tables()}}.
}
\seealso{
Other OMOP-ES extract summary report:
\code{\link[=omop_column_empty]{omop_column_empty()}},
\code{\link[=omop_es_cross_tabulations_html]{omop_es_cross_tabulations_html()}},
\code{\link[=omop_es_data_provenance_html]{omop_es_data_provenance_html()}},
\code{\link[=omop_es_extract_summary_all_tables]{omop_es_extract_summary_all_tables()}},
\code{\link[=omop_es_extract_summary_report]{omop_es_extract_summary_report()}},
\code{\link[=omop_es_field_level_summary_html]{omop_es_field_level_summary_html()}},
\code{\link[=omop_es_markdown_docs_private_html]{omop_es_markdown_docs_private_html()}},
\code{\link[=omop_es_markdown_docs_public_html]{omop_es_markdown_docs_public_html()}},
\code{\link[=suppress_and_format_number]{suppress_and_format_number()}}
}
\concept{OMOP-ES extract summary report}
