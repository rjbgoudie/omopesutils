% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapping-report.R
\name{decorate_mapping_table}
\alias{decorate_mapping_table}
\title{Annotate the concept ids of a mapping table}
\usage{
decorate_mapping_table(
  mapping_table,
  db,
  concept_id_column = guess_concept_id_column(mapping_table)
)
}
\arguments{
\item{mapping_table}{A concept mapping table: a data frame or lazy \code{tbl}
with at least one \verb{*_concept_id} column}

\item{db}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object containing the OMOP vocabulary, in
particular a \code{dbo.concept} table}

\item{concept_id_column}{Name of the column holding the concept ids to
annotate. Defaults to the first column whose name contains
\code{"concept_id"}.}
}
\value{
A table with the same columns as \code{mapping_table}, in which
\code{concept_id_column} holds HTML rather than concept ids.
}
\description{
Replaces the concept ids in a mapping table with an HTML description of
each concept, looked up from the OMOP vocabulary: a link to the concept on
Athena, its name, and coloured pills for its vocabulary, domain and
standard-concept status.
}
\details{
The mapping table is joined to the \code{concept} table on
\code{concept_id_column == concept_id} (with \code{copy = TRUE}, so a local mapping
table can be joined against a vocabulary held in the database). The
concept id column is then rewritten in place, and the columns of the
original mapping table are selected again --- so the result has exactly the
columns it started with, and the extra vocabulary columns used to build the
annotation are dropped.

The result is HTML, so it is intended for rendering rather than for further
analysis. See \code{\link[=gt_decorate_mapping_table]{gt_decorate_mapping_table()}} for a version that returns a
formatted table.
}
\seealso{
Other concept mapping tables:
\code{\link[=gt_decorate_mapping_table]{gt_decorate_mapping_table()}},
\code{\link[=guess_concept_id_column]{guess_concept_id_column()}},
\code{\link[=mapping_table_report]{mapping_table_report()}},
\code{\link[=pretty_athena_link]{pretty_athena_link()}},
\code{\link[=pretty_concept_table]{pretty_concept_table()}},
\code{\link[=pretty_pill]{pretty_pill()}}
}
\concept{concept mapping tables}
