% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diff-counts.R
\name{omop_diff_tables_row_count}
\alias{omop_diff_tables_row_count}
\title{Compare row counts of every OMOP table between two schemas}
\usage{
omop_diff_tables_row_count(db, schema_left = "dbo", schema_right = "dbo2")
}
\arguments{
\item{db}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object holding both extracts, as
registered by two calls to \code{\link[=duckdb_register_omop_es_output]{duckdb_register_omop_es_output()}}}

\item{schema_left}{Name of the schema holding the left-hand (baseline)
public OMOP tables}

\item{schema_right}{Name of the schema holding the right-hand (comparison)
public OMOP tables}
}
\value{
A tibble with one row per OMOP table and columns \code{table},
\code{left_row_count}, \code{right_row_count} and \code{change}, where \code{change} is
\code{right_row_count - left_row_count}.
}
\description{
Counts the rows of each OMOP table in two schemas of the same database and
returns them side by side, with the difference. This gives a quick,
cheap overview of where two extracts differ before looking at individual
rows.
}
\details{
The two sets of counts are combined with a \code{\link[dplyr:full_join]{dplyr::full_join()}}, so a table
present in only one of the schemas appears with an \code{NA} count (and hence an
\code{NA} change) for the other.
}
\seealso{
\code{\link[=omop_diff_plugins_row_count]{omop_diff_plugins_row_count()}} to break the counts down by
OMOP-ES plugin.

Other row counts:
\code{\link[=omop_diff_plugins_row_count]{omop_diff_plugins_row_count()}},
\code{\link[=omop_plugin_row_count]{omop_plugin_row_count()}},
\code{\link[=omop_tables_row_count]{omop_tables_row_count()}}
}
\concept{row counts}
\keyword{internal}
