% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diff-shiny.R
\name{omop_es_diff_viewer}
\alias{omop_es_diff_viewer}
\title{Browse the differences between two OMOP-ES extracts}
\usage{
omop_es_diff_viewer(
  conn,
  schema_public_before = "dbo",
  schema_private_before = "priv",
  schema_public_after = "dbo2",
  schema_private_after = "priv2",
  links_patient_id_column
)
}
\arguments{
\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object holding both extracts}

\item{schema_public_before, schema_private_before}{Names of the schemas
holding the "before" (baseline) public OMOP tables and private \verb{_links}
tables}

\item{schema_public_after, schema_private_after}{Names of the schemas
holding the "after" (comparison) public OMOP tables and private \verb{_links}
tables}

\item{links_patient_id_column}{Name of the patient identifier column in the
OMOP-ES \code{person} \verb{_links} table, without the \code{links__person__} prefix.
Used to label and populate the patient picker. Required.}
}
\value{
A shiny app object, as returned by \code{\link[shiny:shinyApp]{shiny::shinyApp()}}.
}
\description{
Launches a \pkg{shiny} application for comparing two OMOP-ES extracts that
have both been registered into the same database, typically by two calls to
\code{\link[=duckdb_register_omop_es_output]{duckdb_register_omop_es_output()}} with different schema names.
}
\details{
The application has three panels:
\itemize{
\item \strong{Plugin Row Counts} --- the output of \code{\link[=omop_diff_plugins_row_count]{omop_diff_plugins_row_count()}},
i.e. row counts per OMOP table and OMOP-ES plugin, side by side with the
change between them
\item \strong{Table Row Counts} --- the output of \code{\link[=omop_diff_tables_row_count]{omop_diff_tables_row_count()}},
i.e. the same thing per table
\item \strong{Details} --- a row-level diff of one OMOP table, rendered by
\code{\link[=daff_compare]{daff_compare()}}, with a sidebar for choosing the table, restricting to
particular patients, and hiding the OMOP-ES columns
}

In the Details panel each side is read with \code{\link[=omop_es_tbl_with_links]{omop_es_tbl_with_links()}} with
\code{drop_omop_foreign_keys = TRUE}, since surrogate keys are not expected to
be stable between pipeline runs. Rows are ordered by the columns whose
names end in \code{datetime} or \code{concept_id}, or contain \code{Key}, and the standard
OMOP columns are moved to the front. On duckdb, each side is materialised
into a table (\code{temp_before} and \code{temp_after}) with \code{\link[=as_table]{as_table()}}, because
otherwise the set difference between the two sides runs out of memory.

The table picker offers every table present in \emph{either} extract (see
\code{\link[=omop_es_tables_in_either_db]{omop_es_tables_in_either_db()}}), so a table that has been added or removed
can still be selected.

The patient identifier column is passed by name rather than hard-coded, and
is looked up as \verb{links__person__<links_patient_id_column>} --- the name
\code{\link[=omop_es_tbl_with_links]{omop_es_tbl_with_links()}} gives it after joining the \code{person} \verb{_links}
table. This keeps identifiable source-system column names out of this open
source package.
}
\examples{
\dontrun{
db <- DBI::dbConnect(duckdb::duckdb())
duckdb_register_omop_es_output(
  db, before_extract_path, omop_es_path,
  schema_public = "dbo", schema_private = "priv"
)
duckdb_register_omop_es_output(
  db, after_extract_path, omop_es_path,
  schema_public = "dbo2", schema_private = "priv2"
)
omop_es_diff_viewer(db, links_patient_id_column = "my_patient_id_column")
}
}
\seealso{
\code{\link[=omop_es_diff_viewer_local_git]{omop_es_diff_viewer_local_git()}}, which runs the pipeline twice
and then calls this; \code{\link[=omop_es_viewer]{omop_es_viewer()}} for browsing a single extract.

Other OMOP-ES extract viewers:
\code{\link[=omop_es_diff_viewer_local]{omop_es_diff_viewer_local()}},
\code{\link[=omop_es_diff_viewer_local_git]{omop_es_diff_viewer_local_git()}},
\code{\link[=omop_es_viewer]{omop_es_viewer()}}
}
\concept{OMOP-ES extract viewers}
