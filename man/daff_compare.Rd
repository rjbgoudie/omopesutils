% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diff-daff.R
\name{daff_compare}
\alias{daff_compare}
\title{Render an HTML diff of two tables}
\usage{
daff_compare(tbl_left, tbl_right, fragment = FALSE)
}
\arguments{
\item{tbl_left, tbl_right}{Lazy \code{tbl} objects (or data frames) to compare}

\item{fragment}{Whether to render an HTML fragment rather than a complete
HTML document. Use \code{TRUE} when embedding the result in a page, such as a
shiny app.}
}
\value{
A character string of HTML.
}
\description{
Collects two lazy tables into memory and renders the difference between
them as HTML, using \pkg{daff}. The HTML marks up added, removed, modified
and moved rows and columns, and is styled by the CSS that
\code{\link[=omop_es_diff_viewer]{omop_es_diff_viewer()}} injects into its shiny app.
}
\details{
Both tables are fully collected, so this should only be called on a query
that has already been narrowed down --- for example to a handful of
patients.
}
\seealso{
\code{\link[=omop_es_diff_viewer]{omop_es_diff_viewer()}}, which displays this.
}
\keyword{internal}
