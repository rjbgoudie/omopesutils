% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocab-download.R
\name{omop_vocab_parquet_url}
\alias{omop_vocab_parquet_url}
\title{URL of a published OMOP vocabulary parquet file}
\usage{
omop_vocab_parquet_url(
  table = "concept",
  tag = "v20260227",
  repo = "SAFEHR-data/omop-vocabs-processed"
)
}
\arguments{
\item{table}{Name of the vocabulary table, e.g. \code{"concept"}. See
\code{\link[=omop_vocab_processed_tables]{omop_vocab_processed_tables()}}.}

\item{tag}{Git tag naming the vocabulary release.}

\item{repo}{GitHub repository publishing the release, as \code{"owner/name"}.}
}
\value{
A length-one character vector containing an HTTPS URL.
}
\description{
Builds the download URL of a single vocabulary table in a
\href{https://github.com/SAFEHR-data/omop-vocabs-processed}{SAFEHR-data/omop-vocabs-processed}
release.
}
\details{
The parquet files are tracked with Git LFS, so they are served from GitHub's
LFS content host, \code{media.githubusercontent.com}, and \strong{not} from
\verb{github.com/<repo>/raw/...}, which returns a small pointer stub instead of
the file itself. That host also sends \verb{Access-Control-Allow-Origin: *}, which
is what allows the download to work from a browser under \pkg{webr}.

\code{tag} pins the vocabulary release. A concept id means nothing without the
vocabulary version it was drawn from, so record the tag alongside any
mappings built against it. The tag is also what makes the download
reproducible: \code{\link[=omop_vocab_dir]{omop_vocab_dir()}} keeps each release in its own directory.
}
\examples{
omop_vocab_parquet_url("concept")
}
\seealso{
Other remote OMOP vocabularies:
\code{\link[=omop_is_webr]{omop_is_webr()}},
\code{\link[=omop_vocab_dir]{omop_vocab_dir()}},
\code{\link[=omop_vocab_download]{omop_vocab_download()}},
\code{\link[=omop_vocab_processed_tables]{omop_vocab_processed_tables()}},
\code{\link[=vocab_assert_parquet]{vocab_assert_parquet()}},
\code{\link[=vocab_parquet_dir]{vocab_parquet_dir()}}
}
\concept{remote OMOP vocabularies}
