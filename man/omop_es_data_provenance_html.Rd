% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-summary.R
\name{omop_es_data_provenance_html}
\alias{omop_es_data_provenance_html}
\title{Build the data provenance sections of the extract summary report}
\usage{
omop_es_data_provenance_html(plugin_metadata)
}
\arguments{
\item{plugin_metadata}{Plugin metadata in table-major form, i.e. the result
of \code{\link[=omop_es_plugins_extract_metadata]{omop_es_plugins_extract_metadata()}} passed through
\code{\link[purrr:list_transpose]{purrr::list_transpose()}}, so that it is named by OMOP table. Each element
is expected to have \code{tables} and \code{sql} entries, each named by plugin.}
}
\value{
A list of \code{\link[htmltools:tagList]{htmltools::tagList()}}s, named by OMOP table.
}
\description{
For each OMOP table, a "Data provenance" section listing the source
database tables each plugin reads, and the SQL each plugin issues.
}
\details{
The source tables are listed per plugin as inline code. The queries are put
inside collapsible \verb{<details>} elements, one per plugin, because they are
long and are usually only wanted when a particular mapping is in question.

Both come from the plugin introspection functions, so this section is a
rendering of what the plugins actually did rather than of what anyone wrote
down. See \code{\link[=omop_es_plugins_extract_sql]{omop_es_plugins_extract_sql()}} and
\code{\link[=omop_es_plugins_extract_tables]{omop_es_plugins_extract_tables()}}.
}
\seealso{
Other OMOP-ES extract summary report:
\code{\link[=omop_column_empty]{omop_column_empty()}},
\code{\link[=omop_es_all_tables_headings_html]{omop_es_all_tables_headings_html()}},
\code{\link[=omop_es_cross_tabulations_html]{omop_es_cross_tabulations_html()}},
\code{\link[=omop_es_extract_summary_all_tables]{omop_es_extract_summary_all_tables()}},
\code{\link[=omop_es_extract_summary_report]{omop_es_extract_summary_report()}},
\code{\link[=omop_es_field_level_summary_html]{omop_es_field_level_summary_html()}},
\code{\link[=omop_es_markdown_docs_html]{omop_es_markdown_docs_html()}}
}
\concept{OMOP-ES extract summary report}
