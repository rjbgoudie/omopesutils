% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocab-download.R
\name{omop_vocab_download}
\alias{omop_vocab_download}
\title{Download published OMOP vocabulary tables}
\usage{
omop_vocab_download(
  tables = omop_vocab_processed_tables(),
  tag = "v20260227",
  root = NULL,
  repo = "SAFEHR-data/omop-vocabs-processed",
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{tables}{Names of vocabulary tables to download. Defaults to every
published table; see \code{\link[=omop_vocab_processed_tables]{omop_vocab_processed_tables()}}.}

\item{tag}{Git tag naming the vocabulary release.}

\item{root}{Directory to place the release directory in. The default is
session-scoped and depends on the platform, as described above.}

\item{repo}{GitHub repository publishing the release, as \code{"owner/name"}.}

\item{overwrite}{Whether to download tables that are already present.}

\item{quiet}{Whether to suppress the per-file progress messages.}
}
\value{
The release directory, invisibly --- the path to pass as
\code{omop_es_path}. See \code{\link[=omop_vocab_dir]{omop_vocab_dir()}}.
}
\description{
Downloads OMOP vocabulary tables as parquet from
\href{https://github.com/SAFEHR-data/omop-vocabs-processed}{SAFEHR-data/omop-vocabs-processed}
and stores them so that they can be registered in duckdb.
}
\details{
The files are written into an \code{omop_metadata/vocabs} subdirectory of
\code{omop_vocab_dir(tag, root)}, one file per table, named so that
\code{\link[=duckdb_register_parquet_dir]{duckdb_register_parquet_dir()}} turns \code{concept.parquet} into a view called
\code{concept}. That is the layout and the naming that
\code{\link[=duckdb_register_omop_es_output]{duckdb_register_omop_es_output()}} already expects of an OMOP-ES checkout,
so the returned path can be used as its \code{omop_es_path}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{vocab_path <- omop_vocab_download("concept")
duckdb_register_omop_es_output(
  db,
  extract_path = "~/omop_es/extract/CUH_EPIC_small_cohort_2026-02-01",
  omop_es_path = vocab_path
)
}\if{html}{\out{</div>}}

Use \code{\link[=duckdb_register_omop_vocabs]{duckdb_register_omop_vocabs()}} instead where there is no extract to
register alongside them.

Downloads are skipped when the file is already present, unless \code{overwrite}
is \code{TRUE}, so calling this again in a session is cheap. Each file is
downloaded to a \code{.part} file next to its destination and only moved into
place once it is complete and has been checked, so an interrupted download
cannot leave a truncated file that a later call mistakes for a finished one.
}
\section{Size}{

These are whole vocabulary tables, and some are large. For the
\code{v20260227} release: \code{concept} is 139 MB, \code{concept_relationship} 273 MB,
\code{concept_ancestor} 298 MB, \code{concept_synonym} 90 MB and \code{drug_strength}
26 MB; the remaining tables are a few kilobytes each. Downloading the whole
set therefore transfers a little over 800 MB.

There is no partial read: whatever is asked for is transferred in full. Ask
for the tables actually needed rather than the default, particularly under
webR, where the files are held in browser memory and a warning is issued.
}

\examples{
\dontrun{
# just the concept table, which is all a mapping tool needs
vocab_path <- omop_vocab_download("concept")

db <- DBI::dbConnect(duckdb::duckdb())
duckdb_register_omop_vocabs(db, vocab_path)
dplyr::tbl(db, DBI::Id(schema = "dbo", table = "concept"))
}
}
\seealso{
\code{\link[=duckdb_register_omop_vocabs]{duckdb_register_omop_vocabs()}} to register what was downloaded.

Other remote OMOP vocabularies:
\code{\link[=omop_is_webr]{omop_is_webr()}},
\code{\link[=omop_vocab_dir]{omop_vocab_dir()}},
\code{\link[=omop_vocab_parquet_url]{omop_vocab_parquet_url()}},
\code{\link[=omop_vocab_processed_tables]{omop_vocab_processed_tables()}},
\code{\link[=vocab_assert_parquet]{vocab_assert_parquet()}},
\code{\link[=vocab_parquet_dir]{vocab_parquet_dir()}}
}
\concept{remote OMOP vocabularies}
