% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocab-download.R
\name{vocab_assert_parquet}
\alias{vocab_assert_parquet}
\title{Check that a downloaded file really is parquet}
\usage{
vocab_assert_parquet(path, url)
}
\arguments{
\item{path}{Path of the downloaded file.}

\item{url}{URL it was downloaded from, for the error message.}
}
\value{
\code{TRUE}, invisibly, or an error.
}
\description{
Every parquet file begins with the four bytes \code{PAR1}. Checking them turns
the most likely download failure --- a URL that serves a Git LFS pointer
stub or an HTML error page rather than the file --- into an error that says
so, instead of a confusing parquet parse failure later on in duckdb.
}
\details{
The offending file is deleted, so that a subsequent call does not mistake it
for a completed download.
}
\seealso{
Other remote OMOP vocabularies:
\code{\link[=omop_is_webr]{omop_is_webr()}},
\code{\link[=omop_vocab_dir]{omop_vocab_dir()}},
\code{\link[=omop_vocab_download]{omop_vocab_download()}},
\code{\link[=omop_vocab_parquet_url]{omop_vocab_parquet_url()}},
\code{\link[=omop_vocab_processed_tables]{omop_vocab_processed_tables()}},
\code{\link[=vocab_parquet_dir]{vocab_parquet_dir()}}
}
\concept{remote OMOP vocabularies}
\keyword{internal}
