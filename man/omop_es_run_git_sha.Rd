% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/git.R
\name{omop_es_run_git_sha}
\alias{omop_es_run_git_sha}
\title{Run OMOP-ES at a particular git branch or commit}
\usage{
omop_es_run_git_sha(
  branch,
  omop_es_path,
  settings_id = "CUH_EPIC_small_cohort",
  cohort_limit = 5000,
  output_parquet = NA,
  zip_output = FALSE,
  custom_dir = NULL,
  fetch = TRUE,
  envvar = callr::rcmd_safe_env()
)
}
\arguments{
\item{branch}{A git branch or SHA to check out and run}

\item{omop_es_path}{Path to OMOP-ES directory}

\item{settings_id}{The OMOP-ES settings to use}

\item{cohort_limit}{The max number of patients to use.}

\item{output_parquet}{Whether to force parquet output, overriding the
format in the OMOP-ES settings. \code{NA} leaves the settings untouched.}

\item{zip_output}{Whether to zip output}

\item{custom_dir}{A custom directory to write the extract to, overriding the
output directory in the OMOP-ES settings. \code{NULL} uses the settings.}

\item{fetch}{Whether to fetch from the remote before merging}

\item{envvar}{A list of environment variables to set in the child process
prior to running the pipeline. This can be used to pass e.g. database
connection details to OMOP-ES}
}
\value{
Called for its side effects. The return value is that of
\code{\link[=omop_es_run]{omop_es_run()}} or, if changes had to be stashed, of dropping the stash.
}
\description{
Checks out \code{branch} in the OMOP-ES git repository at \code{omop_es_path}, brings
it up to date with its upstream branch, and then runs the pipeline with
\code{\link[=omop_es_run]{omop_es_run()}}.
}
\details{
The steps are:
\enumerate{
\item If the repository has uncommitted changes, they are stashed (including
untracked files) so that the checkout can proceed. They are restored at
the end.
\item \code{branch} is checked out.
\item Unless \code{fetch = FALSE}, the remote is fetched.
\item The upstream branch of the checked-out branch is merged in. If that merge
would not be a fast-forward, the function aborts --- this function only
ever moves a branch forwards, and will not attempt to resolve
divergence.
\item The pipeline is run via \code{\link[=omop_es_run]{omop_es_run()}}, which runs it in a separate
process.
\item Any stashed changes are popped and the stash entry dropped.
}

Note that this mutates the working tree at \code{omop_es_path}: on return the
repository is left on \code{branch}, not on whatever branch it started on.
}
\examples{
\dontrun{
omop_es_run_git_sha(
  branch = "my-feature-branch",
  omop_es_path = "~/omop_es"
)
}
}
\seealso{
\code{\link[=omop_es_diff_viewer_local_git]{omop_es_diff_viewer_local_git()}}, which uses this to run two
branches and diff their output.

Other running OMOP-ES:
\code{\link[=omop_es_main_cuh_interactive]{omop_es_main_cuh_interactive()}},
\code{\link[=omop_es_run]{omop_es_run()}}
}
\concept{running OMOP-ES}
