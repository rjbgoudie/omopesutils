% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocab-download.R
\name{omop_vocab_dir}
\alias{omop_vocab_dir}
\title{Directory in which downloaded OMOP vocabularies are kept}
\usage{
omop_vocab_dir(tag = "v20260227", root = NULL)
}
\arguments{
\item{tag}{Git tag naming the vocabulary release.}

\item{root}{Directory to place the release directory in. The default is
session-scoped and depends on the platform, as described above.}
}
\value{
A path, which may not exist yet.
}
\description{
Where \code{\link[=omop_vocab_download]{omop_vocab_download()}} puts a vocabulary release. The returned path
imitates an OMOP-ES checkout, so it can be passed straight to
\code{\link[=duckdb_register_omop_es_output]{duckdb_register_omop_es_output()}} as its \code{omop_es_path}.
}
\details{
The default location depends on where R is running.

\strong{Under webR}, in a browser, the files go under \verb{/home/web_user}, webR's
home directory. webR's filesystem is held in browser memory, so a download
lasts for the session but is lost on reload, and a large table is a large
amount of memory --- see the warning in \code{\link[=omop_vocab_download]{omop_vocab_download()}}.

\strong{Everywhere else} the files go under \code{\link[base:tempdir]{base::tempdir()}}, and so are cleaned up
when the session ends. Pass \code{root} to keep them somewhere durable instead;
a directory per release means several releases can coexist.

The release \code{tag} is the final path component either way, so downloads for
different vocabulary versions never overwrite one another.
}
\examples{
omop_vocab_dir()
omop_vocab_dir(root = "~/omop-vocabs")
}
\seealso{
Other remote OMOP vocabularies:
\code{\link[=omop_is_webr]{omop_is_webr()}},
\code{\link[=omop_vocab_download]{omop_vocab_download()}},
\code{\link[=omop_vocab_parquet_url]{omop_vocab_parquet_url()}},
\code{\link[=omop_vocab_processed_tables]{omop_vocab_processed_tables()}},
\code{\link[=vocab_assert_parquet]{vocab_assert_parquet()}},
\code{\link[=vocab_parquet_dir]{vocab_parquet_dir()}}
}
\concept{remote OMOP vocabularies}
