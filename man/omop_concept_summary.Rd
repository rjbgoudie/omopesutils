% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omop-cross-tabs.R
\name{omop_concept_summary}
\alias{omop_concept_summary}
\title{Count concept values in every concept column of an OMOP table}
\usage{
omop_concept_summary(db, table)
}
\arguments{
\item{db}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object with an OMOP-ES extract
registered, as by \code{\link[=duckdb_register_omop_es_output]{duckdb_register_omop_es_output()}}.}

\item{table}{OMOP table name, e.g. \code{"condition_occurrence"}.}
}
\value{
A lazy \code{tbl} with one row per column and value, with columns
\code{name}, \code{value}, \code{n}, the joined \code{concept} columns, and \code{table}.
}
\description{
Pivots all of a table's columns whose name contains \code{concept} into long
form, counts how often each value occurs, and joins the OMOP \code{concept}
table so that each value is described.
}
\details{
If the table has no concept columns the pivot fails, in which case a
zero-row result is returned instead --- derived from \code{person}, purely so
that the caller can still union it with the other tables in duckdb.

Nothing in the package currently calls this;
\code{\link[=omop_table_cross_tabulation]{omop_table_cross_tabulation()}} is the routine the summary report uses,
and it tabulates concept, source-value and source-concept columns together
rather than each concept column on its own.
}
\seealso{
Other concept cross-tabulation:
\code{\link[=omop_concept_column_summaries]{omop_concept_column_summaries()}},
\code{\link[=omop_concept_summary_all]{omop_concept_summary_all()}},
\code{\link[=omop_cross_tabulation]{omop_cross_tabulation()}},
\code{\link[=omop_table_cross_tabulation]{omop_table_cross_tabulation()}}
}
\concept{concept cross-tabulation}
\keyword{internal}
