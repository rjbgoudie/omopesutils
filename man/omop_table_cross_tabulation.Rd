% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omop-cross-tabs.R
\name{omop_table_cross_tabulation}
\alias{omop_table_cross_tabulation}
\title{Cross-tabulate the concept columns of one OMOP table}
\usage{
omop_table_cross_tabulation(db, table)
}
\arguments{
\item{db}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object with an OMOP-ES extract
registered, as by \code{\link[=duckdb_register_omop_es_output]{duckdb_register_omop_es_output()}}.}

\item{table}{OMOP table name, e.g. \code{"condition_occurrence"}.}
}
\value{
A lazy \code{tbl} with one row per column stem and concept
combination, with columns \code{n}, \code{column}, \code{concept_id}, \code{source_value},
\code{source_concept_id} and \code{table}, plus the columns of the \code{concept} table
joined on \code{concept_id} and, where the table has one, those of the
\code{concept} table joined on \code{source_concept_id} and prefixed \code{source_}.
}
\description{
Counts how often each combination of concept id, source value and source
concept id occurs in an OMOP table, once for each of its concept columns,
and describes each concept by joining the OMOP \code{concept} table.
}
\details{
OMOP records a mapped concept as a group of columns sharing a stem: for the
stem \code{condition}, the columns \code{condition_concept_id},
\code{condition_source_value} and \code{condition_source_concept_id}. The stems are
found by \code{\link[=omop_table_concept_columns]{omop_table_concept_columns()}}, and each is tabulated in turn and
the results stacked, so one row of the output says "this concept, from this
source value, occurred this many times in this column of this table".

The three columns are renamed to \code{concept_id}, \code{source_value} and
\code{source_concept_id}, which is what makes results from different stems and
different tables stackable; \code{column} records the stem they came from and
\code{table} the table. Not every stem has a \verb{_source_concept_id} column in
OMOP, so that one is joined only where it exists, and its concept columns
are prefixed \code{source_} to keep them apart from those of \code{concept_id}.

A table with no concept columns at all yields a zero-row result with the
same columns, so that it can still be stacked with the others.
}
\seealso{
Other concept cross-tabulation:
\code{\link[=omop_concept_column_summaries]{omop_concept_column_summaries()}},
\code{\link[=omop_concept_summary]{omop_concept_summary()}},
\code{\link[=omop_concept_summary_all]{omop_concept_summary_all()}},
\code{\link[=omop_cross_tabulation]{omop_cross_tabulation()}}
}
\concept{concept cross-tabulation}
