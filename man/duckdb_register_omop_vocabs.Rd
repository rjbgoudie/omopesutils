% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocab-download.R
\name{duckdb_register_omop_vocabs}
\alias{duckdb_register_omop_vocabs}
\title{Register downloaded OMOP vocabularies as duckdb views}
\usage{
duckdb_register_omop_vocabs(con, omop_es_path, schema = "dbo")
}
\arguments{
\item{con}{A database connection}

\item{omop_es_path}{Path to an OMOP-ES directory, or one created by
\code{\link[=omop_vocab_download]{omop_vocab_download()}}, whose \code{omop_metadata/vocabs} subdirectory holds
the vocabulary parquet files}

\item{schema}{Name of schema to register the tables in}
}
\value{
Called for its side effect of registering views on \code{con}. The
return value is that of \code{\link[=duckdb_register_parquet_dir]{duckdb_register_parquet_dir()}} and should not be
relied upon.
}
\description{
Registers the vocabulary parquet files in an OMOP-ES-shaped directory as
duckdb views, so that they can be queried with \pkg{dplyr} and
\pkg{dbplyr}. Nothing is copied: each table becomes a view over the file on
disk.
}
\details{
This registers only the vocabulary tables, which is the part of
\code{\link[=duckdb_register_omop_es_output]{duckdb_register_omop_es_output()}} that does not need an extract. Use it
with a directory from \code{\link[=omop_vocab_download]{omop_vocab_download()}} when the vocabularies are
wanted on their own --- to build or review concept mappings, say --- and
\code{\link[=duckdb_register_omop_es_output]{duckdb_register_omop_es_output()}} when there is an extract to register
beside them.

The view name is the file name with its extension removed, lower-cased, so
\code{concept.parquet} becomes the view \code{concept}. The schema is created if it
does not already exist, but existing contents are left alone.
}
\examples{
\dontrun{
vocab_path <- omop_vocab_download(c("concept", "vocabulary", "domain"))

db <- DBI::dbConnect(duckdb::duckdb())
duckdb_register_omop_vocabs(db, vocab_path)
dplyr::tbl(db, DBI::Id(schema = "dbo", table = "concept"))
}
}
\seealso{
\code{\link[=omop_vocab_download]{omop_vocab_download()}}, which produces a suitable
\code{omop_es_path}.

Other OMOP-ES database registration:
\code{\link[=duckdb_register_omop_es_datalake]{duckdb_register_omop_es_datalake()}},
\code{\link[=duckdb_register_omop_es_output]{duckdb_register_omop_es_output()}},
\code{\link[=duckdb_register_omop_es_single_batch]{duckdb_register_omop_es_single_batch()}},
\code{\link[=duckdb_register_parquet_dir]{duckdb_register_parquet_dir()}}
}
\concept{OMOP-ES database registration}
