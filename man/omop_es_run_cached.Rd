% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diff-cache.R
\name{omop_es_run_cached}
\alias{omop_es_run_cached}
\title{Run OMOP-ES at a branch, reusing a cached extract where possible}
\usage{
omop_es_run_cached(
  branch,
  omop_es_path,
  settings_id = "CUH_EPIC_small_cohort",
  cohort_limit = 5000,
  output_parquet = TRUE,
  fetch = TRUE,
  refresh = FALSE,
  envvar = callr::rcmd_safe_env(),
  label = branch
)
}
\arguments{
\item{branch}{A git branch, tag or SHA to run}

\item{omop_es_path}{Path to OMOP-ES directory}

\item{settings_id}{The OMOP-ES settings to use}

\item{cohort_limit}{The max number of patients to use.}

\item{output_parquet}{Whether to force parquet output, overriding the
format in the OMOP-ES settings. \code{NA} leaves the settings untouched.}

\item{fetch}{Whether to fetch from the remote before merging}

\item{refresh}{Whether to ignore any existing cache entry and run the
pipeline again, replacing it}

\item{envvar}{A list of environment variables to set in the child process
prior to running the pipeline. This can be used to pass e.g. database
connection details to OMOP-ES}

\item{label}{A short description of this side of the comparison, used in
the progress messages}
}
\value{
The path of the extract, cached or freshly produced.
}
\description{
Runs the pipeline for one side of a comparison, unless an extract for the
same commit and the same run options has already been produced, in which
case that one is used and nothing is run.
}
\details{
The commit is resolved with \code{\link[=git_resolve_run_sha]{git_resolve_run_sha()}} before anything is
checked out, so a cache hit avoids touching the working tree at all. On a
miss, the pipeline runs into a staging directory which is moved into place
only once it has completed; a cache entry is therefore either complete or
absent, never half-written.

The cache is keyed on the commit and the run options, and so does not know
about the \emph{source data}. If the source database has changed since a cached
run, the cached extract is stale and \code{refresh = TRUE} is needed to rebuild
it. Nor does it account for \code{envvar}, which can point a run at an entirely
different database.
}
\seealso{
Other cached pipeline runs:
\code{\link[=diff_cache_entry]{diff_cache_entry()}},
\code{\link[=diff_cache_read]{diff_cache_read()}},
\code{\link[=diff_cache_write]{diff_cache_write()}},
\code{\link[=diff_extract_subdir]{diff_extract_subdir()}},
\code{\link[=git_resolve_run_sha]{git_resolve_run_sha()}}
}
\concept{cached pipeline runs}
\keyword{internal}
